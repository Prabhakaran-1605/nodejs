//ES5 //const os = require('os')
//ES6 // import os from 'os'

let os = require("os")                // OS is one of the inbuilt Module in nodejs 
                                      // The node:os module provides operating system-related utility methods and properties. 
                                     //   It can be accessed using:
console.log(os.arch())
console.log(os.platform())
console.log(os.homedir())
console.log(os.version())
console.log(os.platform())
console.log(os.machine())


// To create package.Json File, ( Command - npm init -y ) should be given in terminal 